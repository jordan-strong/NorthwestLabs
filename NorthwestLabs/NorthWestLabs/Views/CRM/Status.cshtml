@model IEnumerable<NorthWestLabs.Models.WorkOrder>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <h2>Work Order Status</h2>
    <h2>123456</h2>
    <table class="table">
        <tr>
            <th>
                Status
            </th>
            <th>
                Date Started
            </th>
            <th>
                Date Finished
            </th>
        </tr>
        <tr>
            <td>
                <div class="progress">
                    <div class="progress-bar" role="progressbar" aria-valuenow="70"
                         aria-valuemin="0" aria-valuemax="100" style="width:20%">
                        20%
                    </div>
                </div>
            </td>
            <td>
                12/01/2016
            </td>
            <td>
                ---
            </td>
            
        </tr>
       
        @*@foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.LTNum)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.customerName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.dateRecieved)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.LTNum }) |
                        @Html.ActionLink("Details", "Details", new { id = item.LTNum }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.LTNum })
                    </td>
                </tr>
            }*@
    </table>
</div>

